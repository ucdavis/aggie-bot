# Ignore sensitive settings
config/settings.yml
config/github.yml

# Do not check Gemfile.lock as directed by the Bundler folks
Gemfile.lock

# Do not check log files into the repository
dss-chatbot.log
chatbot-customers.log
